import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Features — BrightBoard | Online Exam & Assessment Platform',
  description: 'Explore enterprise-grade features for online exams, student management, wallet systems, and affiliate networks. Built for schools, institutes, and teachers.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
